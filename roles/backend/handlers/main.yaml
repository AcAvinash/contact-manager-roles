# roles/backend/handlers/main.yml
- name: Restart backend
  ansible.builtin.shell: |
    pm2 restart backend --update-env || pm2 start {{ backend_workdir }}/server.js --name backend --update-env
  args:
    chdir: "{{ backend_workdir }}"
  become_user: "{{ app_user }}"
 
  
